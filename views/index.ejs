<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video App</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            background-color: #f0f2f5;
            color: #333;
            min-height: 100vh;
        }

        #videoDiv {
            display: grid;
            grid-gap: 10px;
            width: 100%;
            max-width: 1200px;
            padding: 10px;
            margin-top: 20px;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        }

        #controls {
            width: 100%;
            max-width: 1200px;
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }

        .control-btn {
            height: 40px;
            width: 100px;
            text-align: center;
            border-radius: 5px;
            outline: none;
            border: none;
            background-color: #007bff;
            color: white;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .control-btn.cancel {
            background-color: #ff4d4d;
        }

        .control-btn:hover {
            background-color: #0056b3;
        }

        .control-btn.cancel:hover {
            background-color: #ff3333;
        }

        video {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border: 1px solid #ddd;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            #videoDiv {
                grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
                padding: 5px;
            }

            .control-btn {
                width: 80px;
                height: 35px;
                font-size: 14px;
            }
        }
    </style>

    <script type="text/javascript">
        var roomID = "<%= RoomId %>";
    </script>

    <script src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js" defer></script>
    <script src="socket.io/socket.io.js" defer></script>
    <script src="index.js" charset="utf-8" defer></script>
</head>

<body>
    <div id="videoDiv"></div>
    
    <!-- Controls for mute, unmute, and cancel call -->
    <div id="controls">
        <button class="control-btn" id="muteBtn" onclick="toggleMute()">Mute</button>
        <button class="control-btn cancel" id="cancelBtn" onclick="cancelCall()">Cancel Call</button>
    </div>

    <div id="userCount">Participants: 1</div> <!-- Display user count -->
</body>
</html>
